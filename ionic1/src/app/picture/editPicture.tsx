import React from "react";
import { Picture } from "../models";

type Props = {
  picture: Picture;
};

const EditPicture = ({ picture }: Props) => {
  return <div>Edit picture</div>;
};

export default EditPicture;
